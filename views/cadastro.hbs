<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro</title>
    <link rel="stylesheet" href="/css/cadastro.css">
</head>

<body>

    <!-- Container principal do formulário -->
    <div class="form-container">
        <h2>Cadastro para Adoção</h2>

        <!-- Formulário que envia os dados via POST para o servidor -->
        <!-- A rota "/cadastro" deve estar configurada no backend para receber esses dados -->
        <form action="/cadastro" method="POST">
            
            <!-- Campo: Nome Completo -->
            <!-- Mapeamento sugerido no MySQL: VARCHAR(255) -->
            <div class="form-group">
                <label for="nome">Nome Completo:</label>
                <input type="text" id="nome" name="nome" placeholder="Digite seu nome completo" required>
            </div>

            <!-- Campo: E-mail -->
            <!-- Mapeamento sugerido no MySQL: VARCHAR(255), UNIQUE -->
            <div class="form-group">
                <label for="email">E-mail:</label>
                <input type="email" id="email" name="email" placeholder="Digite seu e-mail" required>
            </div>

            <!-- Campo: Número de Celular -->
            <!-- Mapeamento sugerido no MySQL: VARCHAR(20) -->
            <div class="form-group">
                <label for="numero">Número de Celular:</label>
                <input type="tel" id="numero" name="numero" placeholder="Digite seu número de celular" required>
            </div>

            <!-- Campo: CPF -->
            <!-- Mapeamento sugerido no MySQL: CHAR(11) ou VARCHAR(14) -->
            <!-- IMPORTANTE: validar CPF no backend antes de salvar -->
            <div class="form-group">
                <label for="cpf">CPF:</label>
                <input type="text" id="cpf" name="cpf" placeholder="Digite seu CPF" required>
            </div>

            <!-- Campo: Animal de Interesse -->
            <!-- Mapeamento sugerido no MySQL: ENUM('cao', 'gato', 'outros') -->
            <div class="form-group">
                <label for="animal">Animal de Interesse:</label>
                <select id="animal" name="animal">
                    <option value="cao">Cão</option>
                    <option value="gato">Gato</option>
                    <option value="outros">Outros</option>
                </select>
            </div>

            <!-- Campo: Data da Visita -->
            <!-- Mapeamento sugerido no MySQL: DATE -->
            <div class="form-group">
                <label for="dataVisita">Data para Visita:</label>
                <input type="date" id="dataVisita" name="dataVisita" required>
            </div>

            <!-- Botão de envio -->
            <div class="form-group">
                <button type="submit" class="btn">Cadastrar</button>
            </div>
        </form>

        <!-- Botão para voltar à página inicial -->
        <a href="/pagina" class="btn">Voltar para o Início</a>
    </div>

</body>

</html>